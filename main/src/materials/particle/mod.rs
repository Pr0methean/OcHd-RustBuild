use crate::image_tasks::task_spec::paint_svg_task;
use crate::texture_base::material::{MaterialGroup, particle};

// val NOTE: SingleLayerParticle = SingleLayerParticle("note", "note", DirtGroundCover.GRASS_BLOCK.highlight)
// val PARTICLES: MaterialGroup = MaterialGroup(NOTE)

pub const ALL_PARTICLES: MaterialGroup = MaterialGroup { members: vec![] };